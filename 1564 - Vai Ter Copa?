#include <stdio.h>
  
int main() {
  
      int N;
      while (scanf("%d",&N)!=EOF){
      if(0<N && N<=100)
      printf("vai ter duas!\n");
      else
      printf("vai ter copa!\n");
      }        
    return 0;
}
